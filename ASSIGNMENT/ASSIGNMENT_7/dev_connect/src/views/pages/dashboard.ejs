<h2>Dashboard</h2>

<a href="/create">Create Post</a>

<ul>
  <% posts.forEach(post => { %>
    <li style="margin-bottom: 20px;">
      <h3><%= post.title %></h3>
      <p><%= post.content %></p>
      <small>By: <%= post.author?.name || "Unknown" %></small>
    </li>
  <% }) %>
</ul>